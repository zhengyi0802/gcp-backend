        <div class="card">
            <div class="pull-right">
                <a class="btn btn-success" href="{{ url()->previous() }}">{{ __('tables.back') }}</a>
            </div>
        </div>

